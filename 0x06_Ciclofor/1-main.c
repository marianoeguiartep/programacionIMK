#include <stdio.h>
#include "main.h"

int main(void)
{
   int numero = 10;
    printf("natural numbers from 1 to 10: ");

    for(int i=0;i<=numero;i++)
    {
        printf("%d, ",i);
    }
    return(0);
}
